# MASTER PROMPT — Build a Production-Quality Reorder Tool

You are an expert senior frontend engineer specializing in vanilla JavaScript, HTML5, CSS, data-table UX, drag-and-drop interfaces, and robust client-side data manipulation.

I want you to build a **complete, production-quality Reorder Tool** as a self-contained web application.

The implementation must be **10/10 quality**: clean, robust, maintainable, logically structured, defensive against edge cases, and fully functional. Do not give me partial snippets or pseudo-code. Give me the **complete final HTML, CSS, and JavaScript** required to run the application.

Do not make me assemble pieces from multiple answers.

---

# 1. PURPOSE

The application is a browser-based table editor/reorder tool.

The user imports tabular book data, views it in a large horizontally scrollable table, edits selected fields, searches/filter rows, changes column order and width, and reorders rows.

The application is primarily designed for Goodreads-style book data.

The interface must work well on desktop and mobile.

No frameworks are required.

Use:

* HTML5
* CSS3
* Vanilla JavaScript
* No React
* No Vue
* No Angular
* No jQuery
* No external UI framework

Keep dependencies to an absolute minimum.

---

# 2. IMPORTANT: BUILD THE WHOLE APPLICATION

Do NOT answer with isolated snippets.

Produce:

1. Complete HTML
2. Complete CSS
3. Complete JavaScript

The three parts must work together immediately.

Every function that is referenced must exist.

Every variable that is referenced must be defined.

Do not leave placeholders such as:

```javascript
// existing code here
```

or:

```javascript
// implement this later
```

or:

```javascript
TODO
```

Do not assume I will know where to insert missing functions.

The result must be a complete runnable application.

---

# 3. DATA MODEL

The application works with a dataset called:

```javascript
fileData
```

Each row should have a structure conceptually equivalent to:

```javascript
{
    id: "...",
    number: 1,
    reorder: 1,
    originalPosition: 0,
    fields: [],
    start_date: "",
    end_date: "",
    record_deleted_flag: ""
}
```

Imported column headings are stored in:

```javascript
importedHeadings
```

The displayed/imported columns are controlled by:

```javascript
columnOrder
```

Column widths are controlled by:

```javascript
columnSizes
```

Use:

```javascript
DEFAULT_COLUMN_WIDTH
```

as the fallback width.

---

# 4. TABLE STRUCTURE

The table must have:

## Fixed columns at the beginning

1. Reorder controls
2. Row number

Then:

## Imported/editable data columns

These come from:

```javascript
columnOrder
```

Then:

## Fixed SCD columns at the end

1. start_date
2. end_date
3. record_deleted_flag

The table must maintain this order consistently between:

* `<colgroup>`
* headers
* body rows

Column widths must remain aligned.

---

# 5. REORDER CONTROLS

Every row must have:

* Up button: ↑
* Numeric position input
* Down button: ↓
* Move-to-position button: →

The controls must operate on the actual `fileData` index, not merely the visible filtered index.

Functions should include equivalents of:

```javascript
move(index, direction)
moveTo(index, position)
updateReorderNumbers()
```

The numeric position must be validated.

Prevent:

* NaN
* negative positions
* zero positions
* positions beyond the dataset
* invalid indexes
* accidental duplicate rows

After reordering:

1. Update row reorder numbers
2. Update original positions where appropriate
3. Re-render the table
4. Preserve the data correctly

---

# 6. ROW DRAG AND DROP

When there is NO search/filter active, rows must be draggable.

When a search term is active, row dragging must be disabled.

Use HTML5 drag and drop.

Implement:

```javascript
attachRowDragHandlers(tr, item)
```

Requirements:

* `dragstart`
* `dragover`
* `drop`
* `dragend`

Use a stable row ID rather than relying only on the DOM index.

On drop:

1. Find source row by ID
2. Find destination row by ID
3. Validate both
4. Move the row
5. Update reorder numbers
6. Update original positions
7. Re-render

Do not accidentally reorder the wrong row when filtering is active.

---

# 7. SEARCH

Provide a search box.

Search should:

* trim whitespace
* convert to lowercase
* search across all row fields
* update the visible rows
* maintain the real `fileData` indexes

For example:

```javascript
const searchable =
    item.fields
        .map(value => String(value || ""))
        .join(" ")
        .toLowerCase();
```

Filtering must never destroy or modify the underlying dataset.

---

# 8. TAP-TO-EDIT SYSTEM

Most editable cells should initially display:

```html
<div class="cell-display">
    value
</div>
```

Clicking the cell should replace the display with an appropriate editor.

After editing:

1. Validate where necessary
2. Update the underlying `item.fields[column]`
3. Save the change
4. Re-render

Create/use a function equivalent to:

```javascript
updateCell(item, column, value)
```

Avoid duplicate event handlers.

Avoid saving the same edit twice through both `change` and `blur` unless this is intentionally controlled.

---

# 9. EXCLUSIVE SHELF

If the normalized heading is:

```text
exclusive shelf
```

clicking the cell must open a `<select>`.

Allowed values:

```text
to-read
ignore
read
currently-reading
```

The current value must be selected.

Changing the selection must update the underlying data.

---

# 10. MY RATING

If the normalized heading is:

```text
my rating
```

use a numeric editor.

Requirements:

* minimum: -10
* maximum: 10
* integer values only

Validate values before saving.

For example:

```javascript
Number.isInteger(value)
```

Do not allow invalid ratings to enter the dataset.

---

# 11. PRIVATE NOTES

If the normalized heading is:

```text
private notes
```

use a `<textarea>`.

Requirements:

* full cell width
* sensible minimum height
* vertically resizable
* preserve line breaks
* save correctly

Do not truncate private notes in the editor.

---

# 12. PREREQUISITE — BOOK ID — GOODREADS

This is extremely important.

If the normalized heading is:

```text
prerequisite - book id - goodreads
```

the cell must support an autocomplete/suggestion system.

The editor should be:

```html
<input type="text">
```

inside:

```html
<div class="prerequisite-wrapper">
```

with:

```html
<div class="prerequisite-suggestions"></div>
```

---

# 13. PREREQUISITE AUTOCOMPLETE BEHAVIOUR

Assume there is a function:

```javascript
getBookTitleSuggestions()
```

which returns objects such as:

```javascript
[
    {
        id: 123,
        title: "Example Book"
    }
]
```

The autocomplete must:

1. Activate while typing
2. Take the text after the last `#`
3. Search book titles case-insensitively
4. Show up to 10 matches
5. Display each suggestion as:

```text
Book Title (12345)
```

6. Allow clicking a suggestion
7. Insert the Goodreads book ID
8. Preserve previously entered IDs
9. Add `#` after the inserted ID
10. Clear the suggestions
11. Keep the input focused

Example.

If the current value is:

```text
12345#67890#Harry
```

and the user selects:

```text
Harry Potter (99999)
```

the result must become:

```text
12345#67890#99999#
```

---

# 14. CRITICAL AUTOCOMPLETE BUG PREVENTION

The autocomplete suggestion click must NOT fail because of:

* event bubbling
* parent click handlers
* `blur` firing before the suggestion click
* undefined `event` variables
* re-rendering too early
* losing the editor reference
* using the wrong DOM element
* stale references
* duplicate event listeners

When handling a suggestion click, explicitly use the event parameter, e.g.:

```javascript
option.addEventListener("mousedown", ...)
```

or another robust strategy that prevents blur from destroying the editor before the selection is processed.

Prefer `mousedown`/`pointerdown` where appropriate because a normal `click` can occur after the input loses focus.

The selection must reliably insert the ID.

Do NOT use an undefined global:

```javascript
event
```

Use:

```javascript
event
```

only when it is explicitly supplied to the handler:

```javascript
(event) => {}
```

---

# 15. AUTOCOMPLETE SAVE BEHAVIOUR

Selecting a suggestion must update the input value immediately.

Do not immediately call `render()` in a way that destroys the editor before the user sees the selected value.

Save safely.

The user must be able to:

1. Type
2. See suggestions
3. Select suggestion
4. See the ID inserted
5. Continue typing another prerequisite
6. Select another suggestion
7. Eventually leave the field
8. Have the final value saved

This sequence must work repeatedly.

---

# 16. HTML ESCAPING / SECURITY

All user/imported data inserted into HTML must be escaped.

Implement:

```javascript
escapeHtml(value)
```

Do NOT directly inject raw imported values into HTML.

For values placed in input attributes, make sure they cannot break the HTML.

Prefer DOM APIs where appropriate.

---

# 17. COLUMN HEADINGS

Use:

```javascript
normalizeText()
```

to compare headings.

For example:

```javascript
normalizeText(importedHeadings[index])
```

must correctly identify:

* exclusive shelf
* my rating
* private notes
* prerequisite - book id - goodreads

The comparison must be case-insensitive and robust to surrounding whitespace.

---

# 18. COLUMN DRAGGING

Imported columns must be draggable horizontally.

Implement:

```javascript
attachDragHandlers(th, columnIndex)
```

Headers must support:

* drag start
* drag over
* drop
* drag end

After a column move:

* update `columnOrder`
* rebuild the column group
* render headers
* render rows
* preserve column widths

Do not confuse column dragging with header sorting.

---

# 19. COLUMN RESIZING

Each imported header must contain:

```html
<span class="resize-handle"></span>
```

Implement:

```javascript
attachResizeHandlers(th, columnIndex)
```

The user must be able to drag the resize handle horizontally.

Requirements:

* sensible minimum width
* update `columnSizes`
* update the corresponding `<col>`
* prevent negative widths
* prevent accidental text selection while resizing
* work with horizontal scrolling

---

# 20. COLUMN SORTING

Clicking an imported header should sort by that column.

Use:

```javascript
sortByColumn(columnIndex, direction)
```

Direction should alternate between ascending and descending.

Dragging a header must NOT accidentally trigger sorting.

Use a drag flag or equivalent robust mechanism.

---

# 21. COLUMN GROUP

Implement:

```javascript
buildColumnGroup()
```

It must generate:

1. Reorder column width: 140px
2. Number column width: 40px
3. Imported columns using `columnSizes`
4. Three SCD columns at 180px each

The `<colgroup>` and table body/header must remain synchronized.

---

# 22. HEADER RENDERING

Implement:

```javascript
renderHeaders()
```

It must:

1. Clear the existing header
2. Rebuild the column group
3. Add fixed columns
4. Add imported columns
5. Add resize handles
6. Attach column drag handlers
7. Attach sorting handlers
8. Add SCD columns

Do not attach duplicate handlers after repeated renders.

---

# 23. MAIN RENDER FUNCTION

Implement a complete:

```javascript
render()
```

The render function must:

1. Clear the table body
2. Read the search term
3. Calculate filtered indexes
4. Generate each row
5. Generate reorder controls
6. Generate imported cells
7. Generate SCD cells
8. Attach tap-to-edit behaviour
9. Attach special editors
10. Attach autocomplete
11. Attach reorder controls
12. Attach row drag/drop
13. Update information/status text

The function must be logically structured and easy to maintain.

Avoid deeply duplicated code.

Prefer helper functions for:

* creating editors
* saving edits
* prerequisite autocomplete
* validation
* cell rendering

---

# 24. IMPORTANT: DO NOT REPEAT THE BUGGY APPROACH

Do NOT create multiple competing prerequisite autocomplete systems.

There must be exactly ONE autocomplete implementation.

Do NOT have:

```javascript
editor.oninput = ...
```

defined in several different places for the same editor.

Do NOT create an unused helper such as:

```javascript
attachPrerequisiteSuggestions()
```

unless it is actually called correctly.

If you create:

```javascript
attachPrerequisiteSuggestions(editor, item, column)
```

then make sure all required data is explicitly passed to it.

Never rely on accidental outer-scope variables.

---

# 25. EVENT HANDLING

Be especially careful with:

* click
* mousedown
* pointerdown
* blur
* change
* input
* dragstart
* dragover
* drop
* dragend

The interface must not produce accidental edits, accidental sorting, or accidental row dragging.

Buttons inside rows must not trigger row dragging.

Inputs must not trigger row movement.

Autocomplete suggestions must be clickable.

---

# 26. CSS REQUIREMENTS

Use clean, organized CSS.

Include styles for:

* root variables
* page
* controls
* buttons
* inputs
* selects
* table
* table container
* sticky headers
* reorder controls
* drag states
* resize handles
* editable cells
* text editors
* private notes
* prerequisite autocomplete
* mobile layout

Use:

```css
box-sizing:border-box;
```

globally.

The table must support very wide datasets.

The table container must support:

* horizontal scrolling
* vertical scrolling
* touch scrolling

Use a large, usable scrollbar.

The prerequisite suggestion box must appear above/over other table content and must not be clipped.

Use appropriate:

```css
z-index
```

and:

```css
overflow:visible
```

on the prerequisite cell/wrapper where necessary.

Do not duplicate CSS rules unnecessarily.

---

# 27. CURRENT CSS DESIGN

Use a simple black/white interface.

Base variables:

```css
:root{
    --primary:#000;
    --bg:#fff;
    --border:#000;
}
```

The visual design should remain clean and functional rather than decorative.

---

# 28. MOBILE

The application must remain usable below 700px.

Use:

```css
@media(max-width:700px)
```

At minimum:

* reduce body padding
* maintain usable controls
* preserve horizontal table scrolling
* keep autocomplete usable

Do not attempt to squeeze every table column onto the screen.

Horizontal scrolling is acceptable and expected.

---

# 29. DATA INTEGRITY

Never accidentally modify the wrong row.

Never use the visible filtered row number as the underlying array index.

Use stable row IDs where appropriate.

When editing:

```javascript
updateCell(item, column, value)
```

must modify the correct row.

When moving:

```javascript
move(realIndex, direction)
```

must operate on the actual underlying array.

---

# 30. UNDO / STATE SAFETY

If the application already uses:

```javascript
snapshotState()
```

retain that functionality.

Use it before destructive/reordering operations where appropriate.

Do not break existing undo/state functionality.

---

# 31. STATUS INFORMATION

Maintain a status element such as:

```javascript
info.textContent =
    `Loaded: ${
        originalFileName || "none"
    } | Rows: ${
        fileData.length
    }`;
```

The exact formatting can be improved, but it must clearly show:

* loaded filename
* total row count

---

# 32. CODE QUALITY REQUIREMENTS

The final code must be:

* readable
* consistently formatted
* modular
* defensive
* logically ordered
* commented where useful
* free from duplicated handlers
* free from undefined variables
* free from dead prerequisite code
* free from obvious race conditions
* free from syntax errors

Do not over-engineer unnecessarily.

Use clear function names.

Avoid global variables unless they are part of the application's existing architecture.

---

# 33. IMPORTANT VALIDATION BEFORE YOU ANSWER

Before providing the final code, mentally test these exact scenarios:

### Scenario A — Normal editing

Click normal text cell → type → leave → value saves.

### Scenario B — Exclusive shelf

Click Exclusive Shelf → select `read` → value saves.

### Scenario C — My Rating

Click My Rating → enter `7` → value becomes 7.

Enter `100` → value becomes 10.

Enter `-100` → value becomes -10.

### Scenario D — Private notes

Click Private Notes → enter multiline text → leave → text saves.

### Scenario E — Prerequisite autocomplete

Click:

```text
Prerequisite - Book Id - Goodreads
```

Type:

```text
Harry
```

Suggestions appear.

Click:

```text
Harry Potter (12345)
```

The input immediately becomes:

```text
12345#
```

The suggestion box closes.

The input remains usable.

### Scenario F — Multiple prerequisite IDs

Start with:

```text
11111#
```

Type another title.

Select ID `22222`.

Result:

```text
11111#22222#
```

### Scenario G — Search

Search for a book.

Correct rows appear.

Underlying `fileData` remains intact.

### Scenario H — Reorder while unfiltered

Move row up/down/to position.

Correct underlying row moves.

### Scenario I — Reorder while filtered

Search is active.

Row drag-and-drop is disabled.

Buttons still operate on the correct underlying row.

### Scenario J — Column resize

Resize a column.

Header, body, and `<colgroup>` remain aligned.

### Scenario K — Column drag

Drag a column.

Column order changes correctly.

Sorting does not accidentally trigger.

### Scenario L — Header sorting

Click a header.

Rows sort ascending.

Click again.

Rows sort descending.

### Scenario M — Repeat rendering

Search, edit, reorder, sort, and resize repeatedly.

No duplicated event handlers.

No progressively worsening behaviour.

No console errors.

---

# 34. OUTPUT FORMAT

Return the final solution in exactly three clearly labelled sections:

## PART 1 — HTML

Complete HTML.

## PART 2 — CSS

Complete CSS.

## PART 3 — JAVASCRIPT

Complete JavaScript.

Each section must contain complete code blocks.

Do not omit functions.

Do not say "continue from previous code".

Do not tell me to merge code manually.

Do not give me patches.

Do not give me "replace this section".

Give me the complete final implementation.

---

# 35. FINAL REQUIREMENT

Treat this as a real software-engineering task, not a code-generation exercise.

Prioritize correctness over brevity.

Before returning the answer, review the JavaScript for:

* undefined variables
* undefined functions
* incorrect event variables
* incorrect scope
* stale DOM references
* duplicate event listeners
* autocomplete click/blur races
* incorrect array indexes
* invalid HTML
* incorrect escaping
* syntax errors
* inconsistent column counts
* incorrect `<colgroup>` alignment

The prerequisite Goodreads autocomplete is especially important.

The user must be able to click a suggestion and have the Goodreads ID reliably inserted into the field.

The final result should be something I can paste into a new project and run without having to reconstruct missing pieces.
